name: Build Custom VSCode for UOS (Debian10 Debug Test Double-click)

on:
  workflow_dispatch:
    inputs:
      branch:
        description: 'Branch to build'
        required: true
        default: 'main'
      version:
        description: 'Package version'
        required: false
        default: '1.116.0'

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.inputs.branch }}
          fetch-depth: 1

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20.x'

      - name: 🔧 Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential pkg-config libgtk-3-dev libxkbfile-dev \
            libkrb5-dev libgbm-dev rpm libx11-dev libxss-dev libxtst-dev \
            libnss3-dev libasound2-dev libglib2.0-dev libcairo2-dev \
            libpango1.0-dev libatk1.0-dev libgdk-pixbuf2.0-dev \
            python3 git wget curl unzip jq

      - name: 🔧 Fix TypeScript scripts
        run: |
          echo 'console.log("Preinstall OK");' > build/npm/preinstall.js
          sed -i 's/"preinstall": "node build\/npm\/preinstall.ts"/"preinstall": "node build\/npm\/preinstall.js"/' package.json
          echo 'console.log("Postinstall OK");' > build/npm/postinstall.js
          sed -i 's/"postinstall": "node build\/npm\/postinstall.ts"/"postinstall": "node build\/npm\/postinstall.js"/' package.json

      - name: 📦 Install dependencies
        run: |
          echo "=========================================="
          echo "📦 Installing dependencies"
          echo "=========================================="
          npm install --legacy-peer-deps

      - name: 📦 Install build tools
        run: |
          npm install -g tsx
          npm install --legacy-peer-deps \
            ternary-stream vscode-gulp-watch esbuild jsonc-parser \
            gulp-merge-json gulp-tsb gulp-sourcemaps gulp-filter \
            gulp-rename gulp-sort 2>/dev/null || true

      - name: 🔧 Fix extensions.ts
        run: |
          cat > build/lib/extensions.ts << 'EOF'
          import { Stream } from "stream";
          const empty: any[] = [];
          function emptyStream(): Stream {
            const s = new Stream.Readable();
            s._read = () => s.push(null);
            return s;
          }
          export function isWebExtension(): boolean { return false; }
          export function getExtensionStream(): Stream { return emptyStream(); }
          export function getExtensionDownloadStream(): Stream { return emptyStream(); }
          export function packageMarketplaceExtensionsStream(): Stream { return emptyStream(); }
          export function packageNativeLocalExtensionsStream(): Stream { return emptyStream(); }
          export function packageNonNativeLocalExtensionsStream(): Stream { return emptyStream(); }
          export function packageLocalExtensionsStream(): Stream { return emptyStream(); }
          export function packageExtensionsStream(): Stream { return emptyStream(); }
          export function packageCopilotExtensionStream(): Stream { return emptyStream(); }
          export function packageCopilotChatExtensionStream(): Stream { return emptyStream(); }
          export function packageCopilotAgentsExtensionStream(): Stream { return emptyStream(); }
          export function fromMarketplace(): any[] { return empty; }
          export async function fetchUrls(urls: string[], options: any): Promise<void> {
            console.log("fetchUrls: disabled");
            return Promise.resolve();
          }
          export function buildExtensionMedia(): void {}
          export function compileExtensionMedia(): void {}
          export function buildExtensionMediaTask(done: any): void { done(); }
          export const builtInExtensions = empty;
          export const shouldDownloadBuiltInExtensions = false;
          export const webBuiltInExtensions = empty;
          export const shouldDownloadWebBuiltInExtensions = false;
          EOF
          echo 'export const builtInExtensions: any[] = [];
          export const shouldDownloadBuiltInExtensions = false;
          export const webBuiltInExtensions: any[] = [];
          export const shouldDownloadWebBuiltInExtensions = false;' > build/lib/builtInExtensions.ts
          echo 'import { Stream } from "stream";
          export async function fetchUrls(urls: string[], options: any): Promise<void> {
            console.log("fetchUrls: disabled");
            return Promise.resolve();
          }' > build/lib/fetch.ts

      - name: 🔧 Fix gulpfile spawn calls
        run: |
          python3 << 'PY_END'
          import re
          with open('build/gulpfile.vscode.ts', 'r') as f:
              content = f.read()
          content = re.sub(r"spawn\('node'", "spawn('tsx'", content)
          content = re.sub(r'spawn\("node"', 'spawn("tsx"', content)
          content = re.sub(r"spawn\(process\.execPath", "spawn('tsx'", content)
          with open('build/gulpfile.vscode.ts', 'w') as f:
              f.write(content)
          print("spawn calls fixed")
          PY_END

      - name: 🔧 Replace copilot.ts
        run: |
          cat > build/lib/copilot.ts << 'EOF'
          export async function prepareBuiltInCopilotExtensionShims(extRoot: string): Promise<void> {
              console.log('Copilot extension shims preparation skipped for UOS build');
              return;
          }
          export function copyCopilotNativeDeps(productRoot: string): void {
              console.log('Copilot native deps copy skipped for UOS build');
          }
          export function getCopilotExcludeFilter(): any {
              return function() { return false; };
          }
          EOF

      - name: 🔧 Disable Crashpad
        run: |
          echo "Disabling Crashpad"
          python3 << 'PY_JSON_END'
          import json
          import os
          if os.path.exists('product.json'):
              with open('product.json', 'r') as f:
                  data = json.load(f)
              data['enableCrashReport'] = False
              if 'crashReporter' not in data:
                  data['crashReporter'] = {}
              data['crashReporter']['disabled'] = True
              with open('product.json', 'w') as f:
                  json.dump(data, f, indent=2)
              print("product.json modified")
          PY_JSON_END

          mkdir -p src/vs/platform/crash/common
          cat > src/vs/platform/crash/common/crashService.ts << 'CRASH_EOF'
          /*---------------------------------------------------------------------------------------------
           *  Copyright (c) Microsoft Corporation. All rights reserved.
           *  Licensed under the MIT License. See License.txt in the project root for license information.
           *--------------------------------------------------------------------------------------------*/
          import { createDecorator } from '../../../base/common/instantiation.js';
          export const ICrashService = createDecorator<ICrashService>('crashService');
          export interface ICrashService { _serviceBrand: undefined; }
          export class CrashService implements ICrashService {
              declare readonly _serviceBrand: undefined;
              constructor() { console.log('Crash service disabled for UOS build'); }
          }
          CRASH_EOF
          echo "crashService.ts created"

      - name: ⚙️ Set env
        run: |
          echo "VSCODE_SKIP_BUILTIN_EXTENSION_DOWNLOAD=1" >> $GITHUB_ENV
          echo "VSCODE_ELECTRON_SKIP_DOWNLOAD=1" >> $GITHUB_ENV
          echo "VSCODE_SKIP_PRELAUNCH=1" >> $GITHUB_ENV
          echo "VSCODE_SKIP_BUNDLED_EXTENSIONS=1" >> $GITHUB_ENV
          echo "ELECTRON_DISABLE_CRASH_REPORTER=1" >> $GITHUB_ENV
          echo "ELECTRON_DISABLE_CRASH_REPORTING=1" >> $GITHUB_ENV

      - name: 🔨 Build VSCode
        run: |
          export NODE_OPTIONS="--max-old-space-size=8192"
          export VSCODE_SKIP_BUILTIN_EXTENSION_DOWNLOAD=1
          export VSCODE_ELECTRON_SKIP_DOWNLOAD=1
          export VSCODE_SKIP_PRELAUNCH=1
          export VSCODE_SKIP_BUNDLED_EXTENSIONS=1
          export ELECTRON_DISABLE_CRASH_REPORTER=1
          export ELECTRON_DISABLE_CRASH_REPORTING=1
          echo "Start: $(date)"
          npx tsx node_modules/gulp/bin/gulp.js vscode-linux-x64 --no-color
          echo "Done: $(date)"

      - name: 🔍 Verify build contents
        run: |
          echo "=========================================="
          echo "🔍 Verify build contents"
          echo "=========================================="

          BUILD_DIR=""
          for dir in "./VSCode-linux-x64" "./vscode/VSCode-linux-x64" "../VSCode-linux-x64"; do
            if [ -d "$dir" ]; then
              BUILD_DIR="$dir"
              echo "Found build directory: $BUILD_DIR"
              break
            fi
          done

          if [ -z "$BUILD_DIR" ]; then
            echo "Build directory not found!"
            exit 1
          fi

          echo "BUILD_DIR=$BUILD_DIR" >> $GITHUB_ENV

      # ============================================================
      # 11. 下载官方 VSCode（用于复制兼容的 node_modules 和扩展）
      # ============================================================
      - name: 📥 Download official VSCode
        run: |
          echo "=========================================="
          echo "📥 Download official VSCode"
          echo "=========================================="

          VERSION="${{ github.event.inputs.version || '1.116.0' }}"
          echo "Downloading VSCode $VERSION..."
          wget -q --show-progress "https://update.code.visualstudio.com/${VERSION}/linux-x64/stable" -O /tmp/vscode-official.tar.gz

          echo "Extracting official VSCode..."
          mkdir -p /tmp/vscode-official
          tar -xzf /tmp/vscode-official.tar.gz -C /tmp/vscode-official/
          OFFICIAL_DIR=$(find /tmp/vscode-official -type d -name "VSCode-linux-x64" | head -1)

          if [ -z "$OFFICIAL_DIR" ]; then
            echo "❌ Failed to extract official VSCode"
            exit 1
          fi

          echo "✅ Official VSCode extracted to: $OFFICIAL_DIR"
          echo "OFFICIAL_DIR=$OFFICIAL_DIR" >> $GITHUB_ENV

      # ============================================================
      # 12. 复制官方 node_modules（兼容 Debian 10）
      # ============================================================
      - name: 📦 Copy official node_modules
        run: |
          echo "=========================================="
          echo "📦 Copy official node_modules (Debian 10 compatible)"
          echo "=========================================="

          BUILD_DIR="${BUILD_DIR:-./VSCode-linux-x64}"
          OFFICIAL_DIR="${OFFICIAL_DIR}"

          # 备份构建的 node_modules（如果需要）
          if [ -d "$BUILD_DIR/resources/app/node_modules" ]; then
            echo "Backing up built node_modules..."
            mv "$BUILD_DIR/resources/app/node_modules" "$BUILD_DIR/resources/app/node_modules.build.backup"
          fi

          # 复制官方 node_modules
          echo "Copying official node_modules..."
          cp -r "$OFFICIAL_DIR/resources/app/node_modules" "$BUILD_DIR/resources/app/"
          echo "✅ Official node_modules copied"

          # 复制官方 out 目录（确保兼容性）
          echo "Copying official out directory..."
          rm -rf "$BUILD_DIR/resources/app/out"
          cp -r "$OFFICIAL_DIR/resources/app/out" "$BUILD_DIR/resources/app/"
          echo "✅ Official out directory copied"

          # 验证关键原生模块
          echo ""
          echo "📌 Verifying native modules from official:"
          for module in @vscode/spdlog @vscode/sqlite3 native-keymap node-pty; do
            if [ -d "$BUILD_DIR/resources/app/node_modules/$module" ]; then
              NODE_FILES=$(find "$BUILD_DIR/resources/app/node_modules/$module" -name "*.node" -type f 2>/dev/null | wc -l)
              echo "   ✅ $module ($NODE_FILES .node files)"
            else
              echo "   ⚠️ $module (not found in official)"
            fi
          done

      # ============================================================
      # 13. 复制官方内置扩展，但保留自定义扩展
      # ============================================================
      - name: 📂 Merge extensions (Official + Custom)
        run: |
          echo "=========================================="
          echo "📂 Merge extensions from official and custom"
          echo "=========================================="

          BUILD_DIR="${BUILD_DIR:-./VSCode-linux-x64}"
          OFFICIAL_DIR="${OFFICIAL_DIR}"

          # 创建扩展目录
          mkdir -p "$BUILD_DIR/resources/app/extensions"

          # 1. 先复制官方内置扩展（基础功能）
          echo "📌 Copying official built-in extensions..."
          if [ -d "$OFFICIAL_DIR/resources/app/extensions" ]; then
            # 复制所有官方扩展，但跳过 test-tech 开头的（保留自定义版本）
            for ext in "$OFFICIAL_DIR/resources/app/extensions"/*; do
              if [ -d "$ext" ]; then
                EXT_NAME=$(basename "$ext")
                # 如果扩展名以 test-tech 开头，跳过（保留自定义版本）
                if [[ "$EXT_NAME" == test-tech.* ]]; then
                  echo "   ⏭️ $EXT_NAME (custom version, skipping official)"
                else
                  echo "   📥 $EXT_NAME (from official)"
                  cp -r "$ext" "$BUILD_DIR/resources/app/extensions/"
                fi
              fi
            done
            echo "   ✅ Official extensions copied"
          fi

          # 2. 复制你的自定义扩展（强制覆盖官方版本）
          echo ""
          echo "📌 Copying custom extensions from source..."
          if [ -d "./extensions" ]; then
            for dir in ./extensions/*; do
              if [ -d "$dir" ]; then
                EXT_NAME=$(basename "$dir")
                # 检查是否是你的自定义扩展
                if [ -f "$dir/package.json" ]; then
                  PUBLISHER=$(jq -r '.publisher // ""' "$dir/package.json" 2>/dev/null)
                  # 如果是你的扩展（publisher 为 test-tech 或其他自定义标识），强制覆盖
                  if [[ "$PUBLISHER" == test-tech* ]] || [[ "$EXT_NAME" == test-tech.* ]] || [[ "$EXT_NAME" == cweijan* ]]; then
                    echo "   📥 $EXT_NAME (custom - overwriting official)"
                    rm -rf "$BUILD_DIR/resources/app/extensions/$EXT_NAME"
                    cp -r "$dir" "$BUILD_DIR/resources/app/extensions/"
                  elif [ ! -d "$BUILD_DIR/resources/app/extensions/$EXT_NAME" ]; then
                    # 如果是未识别的扩展且官方没有，也复制
                    echo "   📥 $EXT_NAME (new extension)"
                    cp -r "$dir" "$BUILD_DIR/resources/app/extensions/"
                  else
                    echo "   ⏭️ $EXT_NAME (official version kept)"
                  fi
                fi
              fi
            done
            echo "   ✅ Custom extensions merged"
          fi

          # 3. 复制预构建扩展（VSIX）
          echo ""
          echo "📌 Installing prebuilt extensions (VSIX)..."
          if [ -d "./prebuilt-extensions" ]; then
            for vsix in ./prebuilt-extensions/*.vsix; do
              if [ -f "$vsix" ]; then
                EXT_NAME=$(basename "$vsix" .vsix)
                echo "   📥 $EXT_NAME (from VSIX)"
                rm -rf "$BUILD_DIR/resources/app/extensions/$EXT_NAME" 2>/dev/null
                mkdir -p "$BUILD_DIR/resources/app/extensions/$EXT_NAME"
                unzip -q "$vsix" -d "$BUILD_DIR/resources/app/extensions/$EXT_NAME"
                if [ -d "$BUILD_DIR/resources/app/extensions/$EXT_NAME/extension" ]; then
                  mv "$BUILD_DIR/resources/app/extensions/$EXT_NAME/extension"/* "$BUILD_DIR/resources/app/extensions/$EXT_NAME/"
                  rm -rf "$BUILD_DIR/resources/app/extensions/$EXT_NAME/extension"
                fi
              fi
            done
            echo "   ✅ Prebuilt extensions installed"
          fi

          # 4. 生成扩展清单
          echo ""
          echo "📌 Generating extension manifest..."
          EXT_DIR="$BUILD_DIR/resources/app/extensions"
          cat > "$EXT_DIR/.extensions.json" << 'MANIFEST_EOF'
          {
            "extensions": [
          MANIFEST_EOF

          for ext in "$EXT_DIR"/*; do
            if [ -d "$ext" ] && [ -f "$ext/package.json" ]; then
              NAME=$(basename "$ext")
              VERSION=$(jq -r '.version // "unknown"' "$ext/package.json" 2>/dev/null)
              PUBLISHER=$(jq -r '.publisher // "unknown"' "$ext/package.json" 2>/dev/null)
              echo "    {\"name\":\"$NAME\",\"version\":\"$VERSION\",\"publisher\":\"$PUBLISHER\"}," >> "$EXT_DIR/.extensions.json"
            fi
          done
          sed -i '$ s/,$//' "$EXT_DIR/.extensions.json"
          echo "  ]" >> "$EXT_DIR/.extensions.json"
          echo "}" >> "$EXT_DIR/.extensions.json"

          echo "   ✅ Extension manifest created"

          # 5. 统计扩展
          echo ""
          echo "📊 Extension statistics:"
          echo "   Total extensions: $(ls -1 $EXT_DIR | wc -l)"

      # ============================================================
      # 14. 复制配置文件到根目录
      # ============================================================
      - name: 📄 Copy config files to root
        run: |
          echo "=========================================="
          echo "📄 Copy config files to root"
          echo "=========================================="

          BUILD_DIR="${BUILD_DIR:-./VSCode-linux-x64}"
          cd "$BUILD_DIR"

          if [ -f "resources/app/package.json" ]; then
            cp "resources/app/package.json" .
            echo "✅ package.json copied to root"
          fi

          if [ -f "resources/app/product.json" ]; then
            cp "resources/app/product.json" .
            echo "✅ product.json copied to root"
          fi

      # ============================================================
      # 15. 创建可双击运行的启动脚本
      # ============================================================
      - name: 🚀 Create double-click executable
        run: |
          echo "=========================================="
          echo "🚀 Create double-click executable"
          echo "=========================================="

          BUILD_DIR="${BUILD_DIR:-./VSCode-linux-x64}"
          cd "$BUILD_DIR"

          # 查找真正的二进制文件
          REAL_EXEC=""
          for name in code code-oss; do
            if [ -f "./$name" ]; then
              REAL_EXEC="$name"
              break
            fi
          done

          if [ -z "$REAL_EXEC" ]; then
            REAL_EXEC=$(find . -maxdepth 1 -type f -executable ! -name "*.so" ! -name "*.sh" ! -name "chrome-*" 2>/dev/null | head -1)
          fi

          if [ -z "$REAL_EXEC" ] || [ ! -f "$REAL_EXEC" ]; then
            echo "❌ No executable found!"
            exit 1
          fi

          echo "✅ Real executable: $REAL_EXEC"
          mv "$REAL_EXEC" "tscode.real"

          cat > tscode << 'WRAPPER_EOF'
          #!/bin/bash
          SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

          export ELECTRON_ENABLE_GPU=0
          export LIBGL_ALWAYS_SOFTWARE=1
          export NODE_OPTIONS="--max-old-space-size=4096"
          export ELECTRON_DISABLE_SANDBOX=1
          export GDK_BACKEND=x11
          export QT_QPA_PLATFORM=xcb
          export DISPLAY=:0
          export ELECTRON_DISABLE_CRASH_REPORTER=1
          export ELECTRON_DISABLE_CRASH_REPORTING=1
          export CRASHPAD_DISABLE=1

          if [ -f "$SCRIPT_DIR/tscode.real" ]; then
              EXEC="$SCRIPT_DIR/tscode.real"
          else
              echo "❌ VSCode executable not found!"
              exit 1
          fi

          USER_DATA_DIR="$SCRIPT_DIR/user-data"
          mkdir -p "$USER_DATA_DIR"

          exec "$EXEC" \
              --no-sandbox \
              --disable-gpu-sandbox \
              --disable-software-rasterizer \
              --disable-gpu \
              --user-data-dir="$USER_DATA_DIR" \
              "$@"
          WRAPPER_EOF

          chmod +x tscode
          chmod +x tscode.real

          # 创建 start-vscode.sh
          cat > start-vscode.sh << 'START_EOF'
          #!/bin/bash
          SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

          export ELECTRON_ENABLE_GPU=0
          export LIBGL_ALWAYS_SOFTWARE=1
          export NODE_OPTIONS="--max-old-space-size=4096"
          export ELECTRON_DISABLE_SANDBOX=1
          export GDK_BACKEND=x11
          export QT_QPA_PLATFORM=xcb
          export DISPLAY=:0
          export ELECTRON_DISABLE_CRASH_REPORTER=1
          export ELECTRON_DISABLE_CRASH_REPORTING=1
          export CRASHPAD_DISABLE=1

          if [ -f "$SCRIPT_DIR/tscode.real" ]; then
              EXEC="$SCRIPT_DIR/tscode.real"
          else
              echo "❌ VSCode executable not found!"
              exit 1
          fi

          echo "=== VSCode for UOS ==="
          USER_DATA_DIR="$SCRIPT_DIR/user-data"
          mkdir -p "$USER_DATA_DIR"

          exec "$EXEC" \
              --no-sandbox \
              --disable-gpu-sandbox \
              --disable-software-rasterizer \
              --disable-gpu \
              --user-data-dir="$USER_DATA_DIR" \
              "$@"
          START_EOF

          chmod +x start-vscode.sh

          # 创建桌面快捷方式
          cat > vscode-uos.desktop << 'EOF'
          [Desktop Entry]
          Version=1.0
          Type=Application
          Name=VSCode for UOS
          Comment=Visual Studio Code for UOS
          Exec=./tscode %F
          Icon=./resources/app/resources/linux/code.png
          Terminal=false
          Categories=Development;IDE
          MimeType=text/plain;inode/directory
          StartupNotify=true
          StartupWMClass=Code
          EOF

          echo "✅ Startup scripts created"

      # ============================================================
      # 16. 打包（修复语法错误）
      # ============================================================
      - name: 📦 Package
        run: |
          echo "=========================================="
          echo "📦 Package"
          echo "=========================================="

          BUILD_DIR="${BUILD_DIR:-./VSCode-linux-x64}"
          VERSION="${{ github.event.inputs.version || '1.116.0' }}"

          chmod +x $BUILD_DIR/tscode 2>/dev/null || true
          chmod +x $BUILD_DIR/start-vscode.sh 2>/dev/null || true
          chmod +x $BUILD_DIR/chrome-sandbox 2>/dev/null || true
          chmod +x $BUILD_DIR/tscode.real 2>/dev/null || true

          echo ""
          echo "📌 Pre-package validation:"
          echo "   tscode.real: $(if [ -f $BUILD_DIR/tscode.real ]; then echo 'OK'; else echo 'MISSING'; fi)"
          echo "   tscode: $(if [ -f $BUILD_DIR/tscode ]; then echo 'OK'; else echo 'MISSING'; fi)"
          echo "   package.json: $(if [ -f $BUILD_DIR/package.json ]; then echo 'OK'; else echo 'MISSING'; fi)"
          echo "   product.json: $(if [ -f $BUILD_DIR/product.json ]; then echo 'OK'; else echo 'MISSING'; fi)"
          echo "   extensions count: $(ls -1 $BUILD_DIR/resources/app/extensions/ 2>/dev/null | wc -l)"
          echo "   node_modules: $(if [ -d $BUILD_DIR/resources/app/node_modules ]; then echo 'OK'; else echo 'MISSING'; fi)"

          echo ""
          echo "📌 Native modules from official:"
          for module in @vscode/spdlog @vscode/sqlite3 native-keymap node-pty; do
            if [ -d "$BUILD_DIR/resources/app/node_modules/$module" ]; then
              NODE_FILES=$(find "$BUILD_DIR/resources/app/node_modules/$module" -name "*.node" -type f 2>/dev/null | wc -l)
              if [ "$NODE_FILES" -gt 0 ]; then
                echo "   ✅ $module ($NODE_FILES .node files)"
              else
                echo "   ⚠️ $module (no .node files)"
              fi
            else
              echo "   ❌ $module"
            fi
          done

          echo ""
          echo "📌 Custom extensions (test-tech.*):"
          # 修复：使用 find 命令并正确处理
          find "$BUILD_DIR/resources/app/extensions" -maxdepth 1 -type d -name "test-tech.*" 2>/dev/null | while read ext; do
            if [ -d "$ext" ]; then
              echo "   ✅ $(basename "$ext")"
            fi
          done

          # 如果没有找到任何 test-tech 扩展，显示提示
          if [ -z "$(find "$BUILD_DIR/resources/app/extensions" -maxdepth 1 -type d -name "test-tech.*" 2>/dev/null | head -1)" ]; then
            echo "   ℹ️ No test-tech.* extensions found"
          fi

          echo ""
          echo "📌 Custom extensions (cweijan.*):"
          find "$BUILD_DIR/resources/app/extensions" -maxdepth 1 -type d -name "cweijan.*" 2>/dev/null | while read ext; do
            if [ -d "$ext" ]; then
              echo "   ✅ $(basename "$ext")"
            fi
          done

          if [ -z "$(find "$BUILD_DIR/resources/app/extensions" -maxdepth 1 -type d -name "cweijan.*" 2>/dev/null | head -1)" ]; then
            echo "   ℹ️ No cweijan.* extensions found"
          fi

          tar -czf "vscode-uos-${VERSION}-double-click.tar.gz" \
            --preserve-permissions \
            -C "$(dirname "$BUILD_DIR")" \
            "$(basename "$BUILD_DIR")"

          echo ""
          echo "✅ Package created:"
          ls -lh vscode-uos-*.tar.gz

      - name: 📤 Upload artifacts
        uses: actions/upload-artifact@v7
        with:
          name: vscode-uos-double-click
          path: vscode-uos-*.tar.gz
          retention-days: 30

      - name: 📊 Build summary
        if: always()
        run: |
          echo "=========================================="
          echo "BUILD SUMMARY"
          echo "=========================================="
          echo "Status: ${{ job.status }}"
          echo "Version: ${{ github.event.inputs.version || '1.116.0' }}"
          echo "=========================================="
          if [ "${{ job.status }}" = "success" ]; then
            echo ""
            echo "✅ Build completed successfully!"
            echo ""
            echo "Package structure:"
            echo "  - VSCode-linux-x64/"
            echo "    ├── tscode (wrapper script)"
            echo "    ├── tscode.real (binary)"
            echo "    └── resources/"
            echo "        └── app/"
            echo "            ├── extensions/  (official + custom merged)"
            echo "            ├── node_modules/ (from official, Debian 10 compatible)"
            echo "            └── out/ (from official)"
            echo ""
            echo "On UOS:"
            echo "  1. tar -xzf vscode-uos-*.tar.gz"
            echo "  2. cd VSCode-linux-x64"
            echo "  3. ./tscode"
          else
            echo "Build failed. Check logs for details."
          fi
          echo "=========================================="