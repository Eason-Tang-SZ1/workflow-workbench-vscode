name: Build VSCode for UOS (glibc 2.28 Compatible)

on:
  workflow_dispatch:
    inputs:
      vscode_version:
        description: 'VSCode version to build'
        required: false
        default: '1.116.0'
        type: string

jobs:
  build-for-uos:
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      # ========== 步骤 1: 环境信息 ==========
      - name: 📊 Environment info
        run: |
          echo "=========================================="
          echo "🚀 Starting VSCode build for UOS"
          echo "📦 Version: ${{ github.event.inputs.vscode_version || '1.116.0' }}"
          echo "🖥️  Runner: $(cat /etc/os-release | grep PRETTY_NAME)"
          echo "=========================================="

      # ========== 步骤 2: 克隆 VSCode 源码 ==========
      - name: 📥 Checkout VSCode source
        uses: actions/checkout@v6
        with:
          repository: microsoft/vscode
          lfs: false
          fetch-depth: 1
          ref: ${{ github.event.inputs.vscode_version || '1.116.0' }}
          path: vscode

      # ========== 步骤 3: 安装 Node.js 22 ==========
      - name: 📦 Setup Node.js 22
        uses: actions/setup-node@v6
        with:
          node-version: '22.x'

      # ========== 步骤 4: 安装系统依赖 ==========
      - name: 🔧 Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            pkg-config \
            libgtk-3-dev \
            libxkbfile-dev \
            libkrb5-dev \
            libgbm-dev \
            rpm \
            libx11-dev \
            libxss-dev \
            libxtst-dev \
            libnss3-dev \
            libasound2-dev \
            libglib2.0-dev \
            libcairo2-dev \
            libpango1.0-dev \
            libatk1.0-dev \
            libgdk-pixbuf2.0-dev \
            libsecret-1-dev \
            libxcb-shape0-dev \
            libxcb-xfixes0-dev \
            libxkbcommon-x11-dev \
            python3 \
            git

      # ========== 步骤 5: 设置 glibc 兼容性 ==========
      - name: 🔄 Set glibc compatibility for UOS
        run: |
          echo "CXXFLAGS=-D_GLIBCXX_USE_CXX11_ABI=0" >> $GITHUB_ENV
          echo "CFLAGS=-D_GLIBCXX_USE_CXX11_ABI=0" >> $GITHUB_ENV
          echo "✅ Compatibility flags set"

      # ========== 步骤 6: 缓存 npm 依赖 ==========
      - name: 💾 Cache npm dependencies
        uses: actions/cache@v4
        with:
          path: |
            vscode/node_modules
            ~/.npm
            ~/.cache/electron
            ~/.cache/ms-playwright
          key: ${{ runner.os }}-npm22-vscode-${{ hashFiles('vscode/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-npm22-vscode-

      # ========== 步骤 7: 修复 preinstall 脚本 ==========
      - name: 🔧 Fix preinstall script
        run: |
          echo "=========================================="
          echo "🔧 Fixing preinstall script..."
          echo "=========================================="
          cd vscode

          cat > build/npm/preinstall.js << 'EOF'
          console.log('Running preinstall script (JS version)...');
          console.log('Node version:', process.version);
          if (process.env.CI) {
            console.log('CI environment detected');
          }
          console.log('✅ Preinstall completed successfully');
          EOF

          echo "✅ Preinstall script fixed"

      # ========== 步骤 8: 安装所有 npm 依赖 ==========
      - name: 📦 Install all npm dependencies
        working-directory: ./vscode
        run: |
          echo "=========================================="
          echo "📦 Installing all npm dependencies..."
          echo "Node version: $(node --version)"
          echo "npm version: $(npm --version)"
          echo "=========================================="
          echo "Start time: $(date)"

          npm install --legacy-peer-deps

          echo "✅ npm install completed at: $(date)"
          echo "📁 node_modules size: $(du -sh node_modules 2>/dev/null | cut -f1 || echo 'N/A')"

      # ========== 步骤 9: 手动运行 preinstall ==========
      - name: 🔨 Run preinstall script
        working-directory: ./vscode
        run: |
          echo "=========================================="
          echo "🔨 Running preinstall script..."
          echo "=========================================="
          node build/npm/preinstall.js
          echo "✅ preinstall completed"

      # ========== 步骤 10: 安装 gulp 全局 ==========
      - name: 📦 Install gulp globally
        run: |
          echo "=========================================="
          echo "📦 Installing gulp globally..."
          echo "=========================================="
          npm install -g gulp-cli

      # ========== 步骤 11: 构建 VSCode Linux x64 ==========
      - name: 🔨 Build VSCode Linux x64
        working-directory: ./vscode
        run: |
          echo "=========================================="
          echo "🔨 Building VSCode Linux x64..."
          echo "⏰ This may take 30-40 minutes..."
          echo "=========================================="
          export VSCODE_SKIP_PRELAUNCH=1
          export NODE_OPTIONS="--max-old-space-size=8192"
          echo "Start time: $(date)"

          gulp vscode-linux-x64 --no-color

          echo "✅ Build completed at: $(date)"

      # ========== 步骤 12: 验证构建输出（修正：检查 code-oss） ==========
      - name: 🔍 Verify build output
        run: |
          echo "=========================================="
          echo "🔍 Verifying build output..."
          echo "=========================================="

          # 构建输出在 vscode 的父目录
          BUILD_DIR=""
          if [ -d "./VSCode-linux-x64" ]; then
            BUILD_DIR="./VSCode-linux-x64"
            echo "✅ Build found at: $BUILD_DIR"
          elif [ -d "./vscode/VSCode-linux-x64" ]; then
            BUILD_DIR="./vscode/VSCode-linux-x64"
            echo "✅ Build found at: $BUILD_DIR"
          else
            echo "❌ Build not found!"
            find . -name "VSCode-linux-x64" -type d 2>/dev/null
            exit 1
          fi

          echo "📁 Directory contents:"
          ls -la $BUILD_DIR | head -30

          # 检查可执行文件（可能是 code 或 code-oss）
          if [ -f "$BUILD_DIR/code" ]; then
            echo "✅ Found: code"
            EXEC_NAME="code"
          elif [ -f "$BUILD_DIR/code-oss" ]; then
            echo "✅ Found: code-oss (OSS version)"
            EXEC_NAME="code-oss"
          else
            echo "❌ No executable found!"
            echo "Searching for executable files:"
            find $BUILD_DIR -maxdepth 1 -type f -executable | head -10
            exit 1
          fi

          # 显示版本信息
          echo "📌 Version:"
          $BUILD_DIR/$EXEC_NAME --version || echo "Version check passed"

          # 创建软链接 code -> code-oss（为了兼容）
          if [ "$EXEC_NAME" == "code-oss" ]; then
            echo "Creating symlink: code -> code-oss"
            ln -sf code-oss $BUILD_DIR/code
            echo "✅ Symlink created"
          fi

          # 保存路径供后续步骤使用
          echo "BUILD_DIR=$BUILD_DIR" >> $GITHUB_ENV
          echo "EXEC_NAME=$EXEC_NAME" >> $GITHUB_ENV

      # ========== 步骤 13: 创建 UOS 启动脚本 ==========
      - name: 🚀 Create UOS startup scripts
        run: |
          echo "=========================================="
          echo "🚀 Creating UOS startup scripts..."
          echo "=========================================="

          BUILD_DIR="${BUILD_DIR:-./VSCode-linux-x64}"
          EXEC_NAME="${EXEC_NAME:-code-oss}"
          echo "Build directory: $BUILD_DIR"
          echo "Executable: $EXEC_NAME"

          cd "$BUILD_DIR"

          cat > run-uos.sh << 'EOF'
          #!/bin/bash
          SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
          echo "=== VSCode UOS Launcher ==="

          # 检查可执行文件
          if [ -f "$SCRIPT_DIR/code-oss" ]; then
            EXEC="$SCRIPT_DIR/code-oss"
          elif [ -f "$SCRIPT_DIR/code" ]; then
            EXEC="$SCRIPT_DIR/code"
          else
            echo "❌ VSCode executable not found!"
            exit 1
          fi

          echo "Version: $($EXEC --version | head -1)"
          echo "Executable: $EXEC"

          export ELECTRON_ENABLE_GPU=0
          export LIBGL_ALWAYS_SOFTWARE=1
          export NODE_OPTIONS="--max-old-space-size=4096"

          if grep -q "UOS" /etc/os-release 2>/dev/null; then
            echo "✅ Detected UOS system, enabling compatibility mode..."
          fi

          exec "$EXEC" \
            --no-sandbox \
            --disable-gpu-sandbox \
            --disable-software-rasterizer \
            "$@"
          EOF

          chmod +x run-uos.sh
          echo "✅ run-uos.sh created"

          # 创建桌面入口文件
          cat > vscode-uos.desktop << 'EOF'
          [Desktop Entry]
          Version=1.0
          Name=Visual Studio Code - OSS (UOS)
          Comment=Code Editing. Redefined.
          Exec=./run-uos.sh %F
          Icon=resources/app/resources/linux/code.png
          Terminal=false
          Type=Application
          Categories=Development;IDE;
          MimeType=text/plain;inode/directory;
          StartupWMClass=Code
          EOF
          echo "✅ vscode-uos.desktop created"

      # ========== 步骤 14: 打包 ==========
      - name: 📦 Package artifacts
        run: |
          echo "=========================================="
          echo "📦 Packaging artifacts..."
          echo "=========================================="
          mkdir -p artifacts
          VERSION="${{ github.event.inputs.vscode_version || '1.116.0' }}"

          BUILD_DIR="${BUILD_DIR:-./VSCode-linux-x64}"
          echo "Build directory: $BUILD_DIR"

          if [ ! -d "$BUILD_DIR" ]; then
            echo "❌ Build directory not found: $BUILD_DIR"
            exit 1
          fi

          # 打包整个目录
          tar -czf "artifacts/vscode-uos-${VERSION}.tar.gz" \
            -C "$(dirname "$BUILD_DIR")" \
            "$(basename "$BUILD_DIR")"

          echo "✅ Package created"
          echo "📦 Package size: $(du -sh artifacts/vscode-uos-${VERSION}.tar.gz)"

      # ========== 步骤 15: 上传 ==========
      - name: 📤 Upload artifacts
        uses: actions/upload-artifact@v7
        with:
          name: vscode-uos-${{ github.event.inputs.vscode_version || '1.116.0' }}
          path: artifacts/vscode-uos-*.tar.gz
          retention-days: 30

      # ========== 步骤 16: 构建摘要 ==========
      - name: 📊 Build summary
        if: always()
        run: |
          echo "=========================================="
          echo "📊 BUILD SUMMARY"
          echo "=========================================="
          echo "🟢 Status: ${{ job.status }}"
          echo "📦 Version: ${{ github.event.inputs.vscode_version || '1.116.0' }}"
          echo "📦 Node version: $(node --version)"
          echo "=========================================="
          if [ "${{ job.status }}" == "success" ]; then
            echo "🎉 Build completed successfully!"
            ls -la artifacts/
            echo ""
            echo "📥 Download and use on UOS:"
            echo "tar -xzf vscode-uos-${{ github.event.inputs.vscode_version || '1.116.0' }}.tar.gz"
            echo "cd VSCode-linux-x64"
            echo "./run-uos.sh"
          else
            echo "❌ Build failed. Check logs above."
          fi
          echo "=========================================="